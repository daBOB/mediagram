{
  "name": "mediagram-player",
  "version": "0.56.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "MEDIAGRAM_PLAYER_ADDR=0.0.0.0:8770 bun --watch run src/index.ts",
    "test": "bun test",
    "lint": "eslint public --max-warnings 0",
    "typecheck": "tsc --noEmit -p .",
    "preview": "bun run scripts/preview.ts",
    "login": "bun run src/login.ts"
  },
  "dependencies": {
    "hls.js": "^1.7.3",
    "teleproto": "1.229.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "latest",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "typescript": "^7.0.2"
  }
}
