{
  "name": "mediagram-player",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "MEDIAGRAM_PLAYER_ADDR=0.0.0.0:8770 bun --watch run src/index.ts",
    "test": "bun test",
    "login": "bun run src/login.ts"
  },
  "dependencies": {
    "hls.js": "^1.7.3",
    "teleproto": "1.229.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
