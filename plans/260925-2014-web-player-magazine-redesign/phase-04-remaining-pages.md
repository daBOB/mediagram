# Phase 4 — Remaining pages

Priority: medium. Status: todo. Starts only after the home checkpoint is approved.

- **Shelf pages** (Movies, Series, Tutorials, Continue, Watchlist, genre): a
  section-opener header (eyebrow + serif title + count), then the poster grid or list.
  Grid/list preference semantics are unchanged.
- **Film page**: the backdrop as a header band, title in Fraunces, tagline deck,
  overview set as a column of Newsreader text, genres as links, actions as today.
- **Series header / season wall**: the same treatment using the show's backdrop.
- **Search**: results in the magazine grid; the query is echoed as the page title.
- **Collections / profiles / System**: re-skinned with the tokens only, no layout
  change.
- **Featured reel**: already dark; it adopts the backdrop when present.
- **Player HUD**: tokens only (fonts, accent). Its controls are unchanged.

Files: `catalog.css`, `library-controls.css`, `film-page.js`, `series-header.js`,
`shelf-view.js`, `search-view.js`, `featured-reel.js`, `playback.css` (tokens only).
