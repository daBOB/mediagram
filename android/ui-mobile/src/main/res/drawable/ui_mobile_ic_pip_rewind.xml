<?xml version="1.0" encoding="utf-8"?>
<!-- Picture-in-picture "skip back" RemoteAction icon — a generic rewind
     glyph stands in for the exact seek amount (the transport bar's own
     ⏪ reads Player.seekBackIncrement, which is not always literally 10s);
     the RemoteAction's own title already states the real amount. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">
    <path
        android:fillColor="#FF000000"
        android:pathData="M11,18V6L2.5,12Z" />
    <path
        android:fillColor="#FF000000"
        android:pathData="M20,18V6L11.5,12Z" />
</vector>
