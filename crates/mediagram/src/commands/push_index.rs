//! `mediagram push-index`: snapshot library.db, upload, pin, unpin previous.

use anyhow::Result;

use crate::config::Config;

pub async fn run(_cfg: &Config) -> Result<()> {
    anyhow::bail!("not yet implemented")
}
