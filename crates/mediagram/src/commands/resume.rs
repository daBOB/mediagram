//! `mediagram resume`: finish pending sets.

use anyhow::Result;

use crate::config::Config;

pub async fn run(_cfg: &Config, _no_push: bool) -> Result<()> {
    anyhow::bail!("not yet implemented")
}
